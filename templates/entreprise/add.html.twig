{% extends 'base.html.twig' %}

{% block title %} Le form entreprise {% endblock %}

{% block body %}

    {% if edit %}
        <h1> Editer une entreprise </h1>
        {% else %}
        <h1> Ajouter une entreprise </h1>
    {% endif %}

    {{ form(formAddEntreprise) }}

{% endblock %}